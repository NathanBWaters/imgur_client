/**
*
* ImageCard
*
*/

import React from 'react';

import styles from './styles.css';

class ImageCard extends React.Component { // eslint-disable-line react/prefer-stateless-function
  render() {
    return (
      <div className={styles.imageCard}>
      	Hello
      </div>
    );
  }
}

export default ImageCard;
